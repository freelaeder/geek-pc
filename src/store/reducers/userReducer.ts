const initialState = {
    // 用于保存用户登录凭据
    credentials: {},
}

export default function userReducers(state = initialState,action:any){

    switch (action.type){
        default:
            return state
    }
}